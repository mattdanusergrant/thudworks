# Rendered audio one-shots — large binaries, regenerable via gen-808-kit.py /
# gen-synth-melodic.py. The app itself synthesizes all sound live and needs none of these.
assets/

# OS / editor cruft
.DS_Store
Thumbs.db
