# THUDWORKS — Session 2026-06-14

#LLM-generated

**Task:** Build the THUDWORKS web beat machine (MVP).

**Status:** done (MVP) — playable in-browser, not yet real-device verified (no headless browser in sandbox).

**What shipped** (`07_projects/thudworks/app/`):
- Pure-vanilla, no-build, ES-module web app. **All sound synthesized live via Web Audio** (no
  samples, no hosting, no licensing) — the key architectural payoff of the sounds research.
- `synth.js`: 9 voices ported from gen-808-kit.py (kick, 808 bass w/ waveshaper sat, snare, closed/open
  hat, clap, cowbell, clave, tom) → master gain → compressor → out.
- `sequencer.js`: lookahead scheduler (25ms timer, 0.12s ahead) with swing on off-beat 16ths.
- `app.js`: 9×16 grid, click-to-toggle + audition, mute-per-track, BPM/Swing/Vol, Clear/Random,
  4 presets (Boom Bap/Trap/House/Funk), 808 bass root-note picker, rAF visual playhead.
- `index.html` + `style.css`: dark/neon theme, mobile-friendly, "THUDWORKS / make dope grooves".

**Verified:** node --check all 3 modules OK; import/export symbols match; index refs resolve.
HTTP smoke unreliable in sandbox (bg server accepts only first connection — env quirk; `/` served
index.html 200). Browser behaviour NOT yet tested (no headless browser).

**Decisions made:**
- Synthesize everything in-browser rather than bundle sample files — solves the redistribution-license
  problem outright and gives instant load / zero assets. Sampled CC0 kits become an optional later bank.
- 808 bass as a single pitched row w/ global root note (kept MVP UI simple vs per-step pitch).

**Blockers:** none. Next: real-device eyeball; optional GitHub Pages deploy (like jabberjawbreaker) so
the Operator can actually play it.
